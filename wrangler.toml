name = "mount-pages-under-subpaths"
main = "src/worker.ts"
compatibility_date = "2024-11-01"

# Route the entire domain through this Worker
routes = [
  { pattern = "yourdomain.com/*" },
  { pattern = "www.yourdomain.com/*" }
]
